"use server";
import { createClient } from "@/lib/supabase/server";
import { calculateHabitStreak } from "./streak";
import { completionRate } from "./rates";
const dateOk=(v:string)=>{if(!/^\d{4}-\d{2}-\d{2}$/.test(v))return false;const [y,m,d]=v.split("-").map(Number),x=new Date(Date.UTC(y,m-1,d));return x.getUTCFullYear()===y&&x.getUTCMonth()===m-1&&x.getUTCDate()===d;};
const add=(v:string,n:number)=>{const d=new Date(`${v}T00:00:00Z`);d.setUTCDate(d.getUTCDate()+n);return d.toISOString().slice(0,10);};const zoneDate=(z:string)=>{try{return new Intl.DateTimeFormat("en-CA",{timeZone:z,year:"numeric",month:"2-digit",day:"2-digit"}).format(new Date);}catch{return null;}};
export async function getHabitDetail(id:string,date:string,timezone:string){if(!dateOk(date)||!timezone.trim()||timezone.length>100||zoneDate(timezone)!==date)return {success:false as const,message:"습관 상세 정보를 불러오지 못했습니다."};const supabase=await createClient();const {data:auth,error:authError}=await supabase.auth.getClaims();const userId=auth?.claims?.sub;if(authError||!userId)return {success:false as const,message:"습관 상세 정보를 불러오지 못했습니다."};const {data:habit,error}=await supabase.from("habits").select("id,user_id,name,schedule_type,days_of_week,target_per_week,start_date,archived_at,created_at,updated_at").eq("id",id).eq("user_id",userId).maybeSingle();if(error||!habit)return {success:false as const,message:"습관 상세 정보를 불러오지 못했습니다."};const {data:rows,error:rowError}=await supabase.from("habit_completions").select("habit_id,completed_date").eq("habit_id",id).eq("user_id",userId).gte("completed_date",habit.start_date).lte("completed_date",date);if(rowError||!rows)return {success:false as const,message:"습관 상세 정보를 불러오지 못했습니다."};return {success:true as const,habit,streakCount:calculateHabitStreak(habit,rows,date),rate7:completionRate(habit,rows,add(date,-6)<habit.start_date?habit.start_date:add(date,-6),date),rate30:completionRate(habit,rows,add(date,-29)<habit.start_date?habit.start_date:add(date,-29),date),rows};}
